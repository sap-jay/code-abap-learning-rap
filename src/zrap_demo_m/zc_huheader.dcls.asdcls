@EndUserText.label: 'Access Control For ZC_HUHEADER'
@MappingRole: true
define role ZC_HUHEADER {
    grant
        select
            on
                ZC_HUHEADER
                    where
                        inheriting conditions from entity ZI_HUHeader;
                        
}