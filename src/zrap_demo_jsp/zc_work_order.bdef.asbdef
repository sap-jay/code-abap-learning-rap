projection;
strict ( 2 );
use draft;
define behavior for ZC_WORK_ORDER alias workorder
use etag

{
  use create;
  use update;
  use delete;

  use action sendInformation;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
}